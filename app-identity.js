/**
 * @description Application configuration constants for Portfolio 60.
 * Single source of truth for version and app name — imported by server,
 * shared constants, and UI components.
 */

/** @type {string} Application display name */
export const APP_NAME = "Portfolio 60";

/** @type {string} Current application version */
export const APP_VERSION = "0.1.7";
